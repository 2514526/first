今天遇到了比较抽象的题目
但之前在sicton上听过
就是在输入的时候网页把输入的文字在一定程度上理解成程式码
如果输入恶意脚本就可以通过简单的输入框直接攻击伺服器
也就是用括号中的（{{}}）双层大括号可以输入脚本
本次是通过指令扫描伺服器中的资料夹找到 flag的资料夹然后用cat读出来然后印出











以下为Gemini总结：
🚀 CTF 題目分析：Server-Side Template Injection (SSTI1)
1. 漏洞本質 (The Essence)
SSTI (伺服器端模板注入) 發生在 Web 應用程式使用「模板引擎」（如 Jinja2, Mako, Twig）來動態生成網頁，且錯誤地將使用者輸入直接拼接進模板字串中時。

正常運作：開發者預設好模板，僅將數據傳入變數。

漏洞發生：開發者直接將使用者的輸入當成模板的一部分進行渲染。

結果：使用者輸入的特殊語法（如 {{ ... }}）會被伺服器當作「程式碼」來執行，而非「純文字」。

2. 三個階段的指令策略與理由
在滲透過程中，我們採取「由淺入深」的探測策略：

第一階段：漏洞確認與配置偵查
Payload: {{ config }}

理由：在 Flask/Jinja2 環境中，config 是一個全域物件，包含了伺服器的配置資訊。

目的：確認 {{ }} 是否會被執行。如果畫面噴出配置字典（如 DEBUG, SECRET_KEY 等），代表漏洞確實存在。

第二階段：全域環境探測
Payload: {{ self.__dict__ }}

理由：試圖查看當前物件（self）所擁有的所有屬性和方法。

失敗原因：現代框架通常會對 self 進行封裝或限制，導致無法直接存取敏感的全域變數。

第三階段：環境變數檢查
Payload: {{ os.environ }}

理由：在容器化部署（如 Docker）中，Flag 經常被存放在作業系統的環境變數裡。

失敗原因：Jinja2 模板環境是一個「沙盒」，預設不會主動 import os，因此會報錯說找不到該名稱。

3. 終極 Payload 的誕生：Python 反射機制
當簡單的變數無法直接存取時，我們必須利用 Python 的 「反射 (Reflection)」 特性，在記憶體中「爬行」尋找可以執行系統指令的方法。

最終 Payload 解析：
{{ self.__init__.__globals__.__builtins__.__import__('os').popen('ls').read() }}

self.__init__：從目前物件的初始化函式開始。

.__globals__：進入該函式所在的全域命名空間。這是一個字典，存放在該環境下所有的變數、函式與模組。

.__builtins__：找到 Python 內建的工具箱。

.__import__('os')：從工具箱裡強行載入原本被禁用的 os 模組。



另：在做的时候出现印出源头但没有印出里面真正的flag

用以下指令：
既然已經看到檔案叫 flag，我們現在要把指令從「列出清單 (ls)」改成「讀取內容 (cat)」。

請輸入這個 Payload：
{{ self.__init__.__globals__.__builtins__.__import__('os').popen('cat flag').read() }}

指令解析：

cat flag：在 Linux 指令中，cat 代表連接並印出檔案內容。















总的来说就是，要过滤（以目前的理解来看的话就是输入的东西要直接视为字串，不能用“变数”存？）不是很清楚，避免误导，以下是AI总结：
資管系學習總結 (Takeaway)
RCE 的威脅：SSTI 是通往 RCE 的捷徑，代表攻擊者已接管伺服器權限。

防禦建議：

永遠不要使用 render_template_string(user_input)。

應使用 render_template("index.html", data=user_input)，讓模板引擎將輸入視為純數據。

Python 強大性：理解 Python 的內建屬性（魔術方法）是高階滲透的基礎。

.popen('ls')：使用 os 模組開啟一個管道並執行系統指令 ls（列出檔案）。

.read()：讀取該指令執行的結果並印在網頁上。
