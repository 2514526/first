今天的题目相对简单但涉及到了个重要的编码概念和cookie的建立逻辑
直接讲过程，题目告诉我flag在cookie，通过查看网站的cookie我没发现cookie栏位有东西
接着我随便输入个账密，进入下个页面，发现cookie栏位有一串东西，尝试64或32解密发现解不出来
Ai告诉我后面的%3D是为了防止读=的时候读错所以用%3D取代，通过转换，成功用64解码得到flag

//cookie的建立方式
在一开始访问网页的时候，不会分配cookie因为没有任何使用，没有资料
在输入后，不论对或错，你就有了身份是个个体，此时分配cookie所以
这就是为什么一开始看不到，只要输入不论对错就会看到cookie存在原因

//可以用来干嘛
如果建立者在cookie中分配使用者权限叫public，我可以用之前的学过的方法，拦截封包改cookie叫做admin
然后发出，就可以提升自己的权限











以下为AI的总结：
🍪 CTF 題目分析：Cookie 操縱與敏感資訊外洩 (Cookie Monster)
1. 核心概念：什麼是 Cookie？
Cookie 是伺服器傳送給瀏覽器並儲存在本地的一小段文字數據。它的初衷是解決 HTTP 「無狀態（Stateless）」的問題，讓伺服器能辨識使用者的身分或紀錄狀態。

狀態建立：通常在首頁是看不到認證 Cookie 的。當使用者進行「登入」或「互動」動作後，伺服器會透過 Set-Cookie 指令將憑證放入使用者的瀏覽器中。

2. 漏洞本質：不安全的敏感資訊儲存
本題的漏洞在於開發者將 Flag (敏感資訊) 經過編碼後，直接存放在客戶端的 Cookie 中。

錯誤假設：開發者以為一般使用者不會去檢查瀏覽器後台，或認為 Base64 編碼足以保護資料。

事實：Cookie 是完全透明且可由使用者竄改的。

3. 關鍵技術細節：解碼與轉換
在處理這類字串時，通常會遇到兩層「包裝」：

第一層：URL Encoding (網址編碼)
現象：字串中出現 %3D, %2B, %2F 等符號。

原理：為了確保數據在 HTTP 標頭中傳輸不產生歧義，特殊符號會被轉義。

要點：%3D 其實就是 =。這是辨識 Base64 的重要線索。

第二層：Base64 Encoding
特徵：字串由大小寫英文字母、數字組成，且結尾常有一或兩個 =（Padding）。

性質：它是一種「編碼」而非「加密」，任何人都能輕易還原。

4. 滲透與分析流程 (Step-by-Step)
觸發 (Trigger)：訪問網頁並嘗試登入（隨意輸入帳密），誘使伺服器發送 Cookie。

偵查 (Recon)：開啟 Web Inspector (F12) -> Application -> Cookies，找到新產生的 Cookie 項目。

提取 (Extract)：複製 Value 欄位的字串（例如：cGljb...%3D%3D）。

還原 (Restore)：

將 %3D 手動或透過工具轉回 =。

得到符合規格的 Base64 字串。

解碼 (Decode)：使用 Linux 指令或 Burp Suite Decoder 進行解碼。

echo "字串==" | base64 -d

5. 實際應用案例 (Real-world Scenarios)
在現實的資安稽核中，我們會檢查 Cookie 是否包含：

明文身分資訊：如 user_id=123 或 role=admin。

權限控制字串：如果修改 Cookie 的 is_admin=0 變為 1 即可進入後台，這就是 權限提升 (Privilege Escalation)。

缺少安全標記：

HttpOnly：若未開啟，攻擊者可透過 XSS 腳本偷走這串 Flag。

Secure：若未開啟，封包可能在不安全的網路環境（如公共 WiFi）被截聽。

💡 資管系技術總結 (Takeaway)
不要信任客戶端：任何存在使用者電腦裡的資料（Cookie, LocalStorage）都是不可信的，絕對不能存放未加密的敏感資料。

編碼不是加密：Base64 只是為了傳輸方便，不具備任何保密功能。

URL 編碼意識：在處理網路封包時，看到 % 開頭的字串要第一時間反應出這是經過 URL 編碼的原始字元。

邏輯總結：
「這題考驗的是資管人的偵探直覺： Flag 不在網頁畫面上，而是在伺服器塞進你口袋（Cookie）的紙條裡。學會辨識 %3D 的真面目，你就能撕開編碼的表象，拿到核心數據！」

