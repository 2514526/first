今天这个有点麻烦
讲讲过程吧等等直接看Ai总结比较准
通过f12发现留在代码中的不明意义的注解
通过rot13转换得到原始含义
发现如果封包包含（X-Dev-Access: yes）则可以跳过密码验证
通过Burp Suite（一个新下载的程式）
运用封包拦截成功运用在账密请求发出去前拦截封包
不管账密如何只要封包中包含（写在账密条目之前）上面的凭证就可以直接进入系统拿到flag
发出篡改的封包成功拿到flag

//注意：需要先关掉拦截动作才能成功打开网页，只有在真正要用的时候才打开
另：Burp Suite会创造一个魔改的chrome来使用，不干扰原系统







以下为Gemini总结：
1. 資訊偵察：HTML 註解中的秘密
漏洞點：開發者在 HTML 原始碼中留下了 `` 註解。

加密邏輯：訊息使用了 ROT13（凱撒密碼的一種，將字母偏移 13 位）進行簡單混淆。

獲得暗號：解密後得到指令：使用 Header X-Dev-Access: yes。

## 🛠️ 2. 工具應用：Burp Suite 的核心功能
代理攔截 (Proxy Intercept)：利用 Burp Suite 內建瀏覽器攔截瀏覽器發出的 POST 請求。

封包竄改 (Packet Manipulation)：在 HTTP 請求的 Header（標頭） 區塊手動注入自定義標頭。

繞過驗證 (Bypass)：伺服器後端存在「偵錯後門」，只要偵測到該標頭即無視密碼正確性，直接授予存取權限。

## 🕵️ 3. 資管系專業觀點：為什麼這很危險？
這題展現了 不安全的認證機制 (Broken Authentication)：

硬編碼的後門：開發者為了方便，在正式環境留下了基於「特定字串」的權限檢查。

過度信任客戶端：伺服器不應該信任來自用戶端的任何 Header 資訊。任何能被用戶修改的資料（Cookie、Header、Body）都不能作為唯一的安全憑證。

## 💡 給 kaole 的技術成長筆記
HTTP 結構：你現在知道了 HTTP 請求分為 Request Line（第一行）、Headers（各類屬性）與 Body（傳輸數據，如 JSON）。

標頭注入：這是 Web 安全中最常見的操作之一。

ROT13 敏感度：以後在 CTF 看到奇怪的英文亂碼，第一直覺就是試試看 ROT13。
