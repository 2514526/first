在今天的题目中，有三个档案
1.脚本
2.对账本
3.加密目标

脚本通过对输入的密码哈希，把杂凑值拿去对照对账本看是否相同来检查密码对错，这里有个重点
由于哈希值无法反推，所以我们不能反推密码是什么，必须知道
题目给了7个可尝试对象
此时可以一个个尝试，但这里选择自动化

通过更改脚本用if遍历可能的资料阵列我们可以逐个调出来导入input，让系统自动多次尝试
流程为，用if遍历将此时密码存入ps，将ps取哈希得到pw，将pw和系统规定的对照得到对错，（若错则下一个）若对则将“ps”和文件丢去解密
这里同时也用到的，密码即解密的重要参数，不存在密码错误这个加密手段


具体操作如下
pos_pw_list = ["f09e", "4dcf", "87ab", "dba8", "752e", "3961", "f159"]

def level_3_pw_check():
    user_pw = input("Please enter correct password for flag: ")
    user_pw_hash = hash_pw(user_pw)
    
    for password in pos_pw_list:
        user_pw_hash = hash_pw(password)
        if( user_pw_hash == correct_pw_hash ):
            print("Welcome back... your flag, user:")
            decryption = str_xor(flag_enc.decode(), password)
            print(decryption)
            return
    print("That password is incorrect")


另外，python用#打注解，不存在多行注解如/**/；python从第一行开始，不存在C那种后面定义的东西
