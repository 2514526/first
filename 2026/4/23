今天是一堆资料，就是资料夹里面还有资料夹等
不能直接用基础的strings 资料 | grep “目标”

判断：是否为纯文字，或是存在二进位档案
  若纯文字，则直接使用grep


步骤为grep -r "picoCTF" big-zip-files/
其中grep为指令工具
-r为动作，意为逐个进去每个资料夹遍历所有项目，和自我删除那个指令一样
“”为目标
最后是目标资料夹，加上 / 代表对其下所有资料执行

若资料夹同时含有大量二进位档案（非txt文字档）则需要 | 前的string来过滤执行

当然这不是今天的重点：）

补：若不确定大小写可以在加一个动作参数i，使用方法为，直接加在r后面，写作-ri，表示忽略大小写，看字对就拉出来
