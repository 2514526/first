新的语法
with....as....
1. 什麼是 with...as？
在處理檔案（File）、資料庫連接或網路通訊時，有一個最重要的原則：「打開了，就一定要記得關掉。」

傳統寫法 (像 C/Java)：
你必須先 f = open(...)，然後在最後手動打 f.close()。如果你程式中間報錯（Error）導致沒跑完，那個檔案就會被鎖死（Memory Leak），直到你重啟電腦。

with...as 寫法：
它保證了不管發生什麼事，只要離開這個程式區塊，系統會自動幫你關閉檔案。

## 🛠️ 2. 語法拆解
Python
with open('file.txt', 'r') as f:
    # 在這個縮排區塊內，檔案是開著的
    data = f.read()

# 一旦縮排結束，檔案就已經自動被 close() 了，非常安全！
with：開啟一個特殊的監控模式。

open(...)：你要執行的動作（打開檔案）。

as f：幫這個開啟的檔案取個臨時變數名稱叫 f。

## 🔍 3. 解釋這幾行的具體作用
你剛才看到的這兩行，是把「硬碟裡的文字」變成「記憶體裡的清單」的關鍵步驟：

第一行：with open('dictionary.txt', 'r') as f:
作用： 以「唯讀模式 ('r')」打開裝滿密碼的字典檔。

隱喻： 把存放密碼的倉庫門打開，並請一位叫 f 的搬運工待命。

第二行：pos_pw_list = [line.strip() for line in f.readlines()]
這行看起來很長，其實用了 Python 的 「串列生成式 (List Comprehension)」，我們可以拆解成三個動作：

f.readlines()：
叫搬運工把檔案裡的每一行都搬出來。這會產生一個像這樣的清單：['password1\n', 'password2\n', ...]。注意最後都有個 \n（換行符號）。

line.strip()：
這是一個「清理」動作。它會把字串前後的空白和換行符號 (\n) 剝掉。這非常重要！如果不剝掉，你拿著帶有換行符號的密碼去比對 Hash，永遠都不會對。

[... for line in ...]：
這是一個快速迴圈。意思是：「把檔案裡的每一行拿出來，清理乾淨後，通通裝進 pos_pw_list 這個大袋子裡。」



在之前的题目中，可能密码存在于原脚本中，也就是已经有阵列可以直接使用
在这里可能密码存在于外面的txt文件里
我们要读入文件然后抓出密码组成阵列
接着就是之前的步骤
