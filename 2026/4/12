学会了档案间的运作逻辑，运用脚本解密一份档案得到flag，通常骇客会把恶意核心逻辑加密储存
此时需要钥匙来启动解密脚本权限才能导入加密文档后给出解密结果
学习了对档案下动作的命令，如 python3 ende.py -d flag.txt.en
就是对“ende.py”这个档案执行“-d”运算的动作，目标是flag.txt.en这个档案
  同时使用python3这个直译器来理解这串脚本
另外，拿到脚本不知道怎么办吗，试试看这个：python3 档名.py -h

  总结：   python3 <檔案> <參數> <對象>
  AI总结：
  檔案            角色            隱喻
  ende.py         邏輯 (Logic),  處理資料的機器（果汁機）。
  flag.txt.en     資料 (Data),   被處理的對象（水果）。
  password.txt    密鑰 (Key),    啟動權限（插座開關）。

***密码的两种形态
1.程式码里面有if（输入密码==密码）true；
    这种状态下，直接打开程式码看源代码就能找到密码，复制出来就能直接解开，也就是直接抢走它手中的答案来回答

2.密码是参数
    在这种状况下密码不存在错误这一说，只要你输入他就给过，但输入的密码会被当成参数放进脚本里和目标搅拌
    只要你输入的密码不是预设想要的，出来的结果就不会是你想到的
        这跟哈希值一样，只要输入不对，密码+目标就！=真正的结果
  
